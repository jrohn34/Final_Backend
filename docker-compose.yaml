services:
  demo-database:
    image: postgres:alpine
    container_name: Final
    ports:
      - "6001:5432"
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: Final